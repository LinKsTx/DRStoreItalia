import { BusquedaBlogPipe } from './busqueda-blog.pipe';

describe('BusquedaBlogPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaBlogPipe();
    expect(pipe).toBeTruthy();
  });
});
